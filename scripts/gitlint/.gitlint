# Chewie Labs gitlint rules
#
# Available rules can be found at:
# https://jorisroovers.com/gitlint/rules/

# Skip all default Title/Body rules comes with gitlint
# For now we're only utilizing gitlint to check mandatory fields
[general]
ignore=T1, T2, T3, T4, T5, T6, T7, T8, B1, B2, B3, B4, B5, B6, B7, B8

[body-match-regex:Check-bug-number]
regex=(.*)(BUG|Bug|bug)(s?):(.*)

#[body-match-regex:Check-test-case]
#regex=(.*)(TEST|Test|test)(s?):.(.*)
